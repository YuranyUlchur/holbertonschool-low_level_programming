#include <stdio.h>
#include <ctype.h>
/**
 *main - Entry point
 *Return: Always 0 (Success)
 */
int main(void)
{
	int x;
	int zero = 48;
	int nine = 57;
	int coma = 44;


	for (x = zero; x <= nine; x++)
	{
		for (nine = x; nine < x; x ++)
		{
			/* code */
		}
		
	}
		if (x == nine)
		putchar(x);
		putchar(coma);
	{
		putchar(' ');

	}



	
	putchar('\n');
	return (0);
}
