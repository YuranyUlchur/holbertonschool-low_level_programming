#include "main.h"
/**
 *add - add two number
 *@a:  value in integer
 *@b: value in integer
 *Return: result the a and b
 */
int add(int a, int b)
{
	int resultado;

	resultado = a + b;
	return (resultado);
}
