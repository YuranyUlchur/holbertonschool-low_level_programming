#include "3-calc.h"
/**
 * op_add - function suma
 * @a: value integer
 * @b: value integer
 * Return: Return the suma
 */

int op_add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
#include "3-calc.h"

/**
 * op_sub - function  the difference
 * @a: value integer
 * @b: value integer
 * Return: Return the resta
 */

int op_sub(int a, int b)
{
	int resta;

	resta = a - b;
	return (resta);
}
#include "3-calc.h"

/**
 * op_mul - function the multiplicacion
 * @a: value integer
 * @b: value integer
 * Return: Return the multiplicacion
 */

int op_mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
#include "3-calc.h"
/**
 * op_div - function the division
 * @a: value integer
 * @b: value integer
 * Return: Return the division
 */
int op_div(int a, int b)
{	int div;

	div = a / b;
	return (div);
}
#include "3-calc.h"
/**
 * op_mod - function the remainder of the division
 * @a: value integer
 * @b: value integer
 * Return: Return the remainder of the division
 */

int op_mod(int a, int b)
{	int remainder;

	remainder = a % d;
	return (remainder);
}
